[project]
name = "llm-sdk"
version = "0.1.0"
description = "Lightweight local LLM SDK with an offline fallback"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["llm_sdk"]
